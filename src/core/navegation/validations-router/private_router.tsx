export default function PrivateRouter({ component: Component, ...rest }: any) {
	// let history = useHistory();
	// var decoded: any
	// useEffect(() => {
	// 	decoded = jwt_decode(getToken()!);
	// 	if (decoded.rol != 'Operador') {
	// 		history.push('/panel');
	// 	}
	// }, [])

	// return (
	// 	<Route {...rest} ><Component /></Route>
	// )
}
