import jwt_decode from 'jwt-decode'

export default function RedirectRol({ component: Component, ...rest }: any) {
	// var decoded: any
	// var redirectRol: any = '/panel/home'
	// decoded = jwt_decode(getToken()!);
	// if (decoded.rol == RoleType.Operador || decoded.rol == RoleType.Admin) {
	// 	redirectRol = '/panel/home'
	// } else if (decoded.rol == RoleType.Coordinador || decoded.rol == RoleType.Consultor) {
	// 	redirectRol = '/panel/audiencias'
	// } else if (decoded.rol == RoleType.Mantenimiento) {
	// 	redirectRol = '/panel/user'
	// }

	// return (<Redirect from='/panel' to={redirectRol} />)
}
